import java.util.Random;

public class RandomWork {
    public static void main(String[] args) {
        Random r = new Random();
            // r.nextInt(high-low) + low;
        System.out.println(r.nextDouble(10-5)+5);
    }
}
